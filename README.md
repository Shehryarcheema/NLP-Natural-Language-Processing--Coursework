This project implements five NLP models—MLP, RNN, GRU, LSTM, and Transformer—within a chatbot to compare their performance on text tasks. It includes preprocessing, training, evaluation, and BLEU-based analysis, enabling the chatbot to generate responses using each model.
